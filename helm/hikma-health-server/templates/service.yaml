apiVersion: v1
kind: Service
metadata:
  name: {{ include "hikma-health-server.fullname" . }}
  labels:
    {{- include "hikma-health-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hikma-health-server.selectorLabels" . | nindent 4 }}
